import java.util.*;
import javax.swing.*;
import java.awt.Color;
class Main
{
    public static void main(String args[])
    {
        // JFrame frame=new JFrame();//creates frame 
        // frame.setTitle("Jframe title goes here     <----- Tilele name");//sets title
        // frame.setSize(420,420);//frame size
        // frame.setVisible(true);//makes frame visible
        // frame.setResizable(false);//prevent from resizing the frame
        // frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);//close program when x button is pressed
        // ImageIcon image=new ImageIcon("oc.png");//create an ImageIcon
        
        // frame.setIconImage(image.getImage());//change icon of frame
        // frame.getContentPane().setBackground(new Color(100,240,150));//change color of  background
        MyFrame myframe=new MyFrame();//creates frame 
    }
}